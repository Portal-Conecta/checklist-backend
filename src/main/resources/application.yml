spring:
  application:
    name: conecta.checklist
  profiles:
    default: local

server:
  port: ${SERVER_PORT:8083}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

checklist:
  cors:
    allowed-origins: ${CHECKLIST_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
  security:
    swagger-public: false
    jwt:
      secret: ${JWT_SECRET}
  timezone: America/Sao_Paulo

hub:
  api:
    url: ${HUB_API_URL}
